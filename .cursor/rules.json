{
  "rules": {
    "nextjspages": "Used for correctly setting up page following nextJS principles for server side before client",
    "tomlconfig": "Saving or reading configuration data to TOML",
    "logging": "Comprehensive logging standards for all new features and modifications. See docs/technical/logging/standards.md for detailed guidelines."
  },
  "globs": {
    "nextjspages": ["src/app/**/*.tsx", "src/app/**/*.ts"],
    "tomlconfig": ["src/lib/config/**/*.ts"],
    "logging": ["src/app/**/*.tsx", "src/app/**/*.ts", "src/lib/**/*.ts"]
  }
}
